ext {
    applicationId = "com.example.aaron.compsapp"

    compileSdkVersion = 28

    minSdkVersion = 23
    targetSdkVersion = 28
    versionCode = 1
    versionName = "1.0"

    //app,task,customer,setting
    runModule = "setting"
    appDependencies = ["task", "customer", "setting"] //app运行时需要依赖的module
    taskDependencies = ["customer"] //task运行时需要依赖的module
    customerDependencies = [] //task运行时需要依赖的module
    settingDependencies = ["task", "customer"] //task运行时需要依赖的module
}
