ext {
    compileSdkVersion = 28

    minSdkVersion = 23
    targetSdkVersion = 28
    versionCode = 1
    versionName = "1.0"

    runModule = "task"
    appDependencies = ["task"] //app运行时需要依赖的module
    taskDependencies = [] //task运行时需要依赖的module

    runDependencies = this["${runModule}Dependencies"] //运行的module需要的依赖module
}